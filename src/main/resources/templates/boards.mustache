<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Boards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
        }
        .board-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }
        .board {
            border: 1px solid #ccc;
            padding: 10px;
            width: 200px;
            text-align: center;
            border-radius: 10px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
        }
        .board a {
            text-decoration: none;
            color: blue;
            font-weight: bold;
        }
    </style>
</head>
<body>

<h1>Select a Board</h1>

<div class="board-container">
    {{#boards}}
        <div class="board">
            <a href="/boards/{{id}}/articles">{{name}}</a>
            <p>{{description}}</p>
        </div>
    {{/boards}}
</div>

</body>
</html>
